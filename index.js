// const { randomNumber, addition } = require("./util");
// console.log(`random number is: ${randomNumber()}`);
// console.log(`${addition(5, 3)}`);

import getPost from "./postController.js";
console.log(getPost());
